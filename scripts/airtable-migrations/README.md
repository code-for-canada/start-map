# Airtable Migrations

This Python script is used to 

## Setup

Requires: `python` and `pip`.

```
pip install pipenv
pipenv install
cp sample.env .env
```

## Running script

```
pipenv run python migrate-airtable.py 
```
